---
title: iOS锁的应用
tags:
  - 标签1
  - 标签2
  - 标签3
categories:
  - 分类1
  - 分类2
comments: false
date: 2020-05-23 15:24:38
---



#### 一、什么是锁

在计算机科学中，锁是一种同步机制，用于多线程环境中对资源访问的限制。

比如：

对于一个资源不能同时进行写操作。否则可能会造成数据混乱，丢失等。

对于一个资源不能同时一个线程在进行写操作，另一个线程在进行读操作。否则另一个线程可能读到的是旧的，甚至是已经被删除的数据。

这些情况都需要锁来进行对资源访问的限制。常见的线程同步技术就是`加锁`。

[线程同步的四种方式](https://blog.csdn.net/guoxiang3538/article/details/79376191) 文中讲解了一些基本概念如什么是线程同步。

#### 二、iOS中的锁

在iOS中，有互斥锁、递归锁、读写锁、自旋锁、条件锁、信号量。

> 对同一个锁对象的加解锁必须保持在同一线程执行，如果尝试在不同线程去加解锁将会引发一个运行时的错误。

**具体各种锁介绍与使用**

Todo 

读写锁

前面mutex锁有个缺点，就是只要锁住了，不管其他线程要干什么，都不允许进入临界区。设想这样一种情况：临界区foo变量在被bar1线程读着，加了个mutex锁，bar2线程如果也要读foo变量，因为被bar1加了个互斥锁，那就不能读了。但事实情况是，读取数据不影响数据内容本身，所以即便被1个线程读着，另外一个线程也应该允许他去读。除非另外一个线程是写操作，为了避免数据不一致的问题，写线程就需要等读线程都结束了再写。

**自旋锁的实现原理**

自旋锁的目的是为了**确保临界区只有一个线程可以访问**，它的使用可以用下面这段伪代码来描述:

```
bool lock = 0; //0代表空闲状态，1代表锁住状态
{
		while (lock); //查询锁的状态，相当于申请锁
		lock = 1; //加锁
		临界区代码
		lock = 0; //解锁
}
```

刚开始时，锁处于空闲状态，线程A申请锁成功，然后加锁。当线程B也申请锁时，锁已经处于锁住状态，因此申请失败，于是线程B开始循环查询锁的状态，当线程A解锁后，线程B检测到锁处于空闲状态，于是申请成功，随即加上锁。

看似没什么问题，但是如果一开始就有两个线程申请锁，那么必然都申请成功，同时执行临界区代码，这样就没有做到对资源的访问限制了。

需要改进：

```c
bool lock = 0;
{
		while (testAndSet(&lock)); //尝试加锁
		临界区代码
		lock = 0; //解锁
}

bool testAndSet(bool *target) {
		bool status = *target;
		*target = true;
		return status;
}
```

这里testAndSet函数是test_and_set汇编指令的伪代码，test_and_set汇编指令是硬件层面保证的原子操作。这段代码的作用是把 target 的值设置为 1，并返回原来的值。如果当前锁为锁住状态，`*target = true;`无影响，尝试加锁失败依旧得忙等着。如果当前锁为空闲状态，则设置为锁住状态，并返回原来的值--空闲状态，于是加锁成功。由于是原子操作，所以不会同时有两个线程执行该函数。

可以看到，自旋锁会让加锁失败的线程进入忙等状态，白白浪费CPU资源。自旋锁的这一特点可以说即是优点也是缺点，衡量的标准就是看忙等耗时的长短，这取决于临界区的场景。如果临界区的代码很简单，那么忙等的时间就会很短，这时自旋锁的效率就很高。如果临界区代码是一些I/O操作，那么忙等的时间就会很长直到被系统抢占，这个时候自旋锁就大大的浪费了CPU资源。

如果低优先级线程先拿到锁，那么高优先级线程就会进入忙等(busy-wait)状态，消耗大量 CPU 时间，如果线程的调度算法是让高优先级线程始终在低优先级线程前执行，那么低优先级线程将拿不到 CPU 时间，也就无法完成任务并释放锁。这种问题被称为优先级反转。这也是iOS中OSSpinLock 不再安全的原因。在 iOS 10/macOS 10.12 发布时，苹果提供了新的 os_unfair_lock 作为 OSSpinLock 的替代，并且将 OSSpinLock 标记为了 Deprecated。

PS：

[不再安全的 OSSpinLock](https://blog.ibireme.com/2016/01/16/spinlock_is_unsafe_in_ios/)，是YYKit的作者写的一篇博客，居然看到了问题的缘由是讨论 weak 属性的线程安全问题，用了这么久的weak属性结果发现weak变量是线程不安全的，有时候会因此而过度释放造成崩溃。参考：[不安全的weak变量](https://blog.csdn.net/rhythmkay/article/details/86539617)。看来得仔细研究下weak修饰符的实现原理。

**信号量的实现原理**



**互斥锁的实现原理**



**锁的实现原理参考**

[iOS线程和锁实现原理分析](https://www.jianshu.com/p/58af1e853900)

[多线程安全？各种锁的实现原理](https://al1020119.github.io/blog/2016/08/31/duo-xian-cheng-an-quan-%3Fge-chong-suo-de-shi-xian-yuan-li/)

[并发原理篇：锁](https://hacpai.com/article/1477323299224)，讲解了一些很基本的概念比如临界区是什么，互斥锁的实现。

[操作系统中锁的实现原理](https://blog.csdn.net/qq_35181209/article/details/78026636)

#### 不使用锁达到线程安全的目的

在 GCD 队列中，有个简单直接的方法可以代替同步锁或锁对象，将读写操作都安排在一个串行队列里，即可保证数据同步.

[Effective Objective-C Notes：GCD 实现同步锁](https://www.zybuluo.com/MicroCai/note/64272)

#### 产生死锁的原因

死锁的四个必要条件

互斥条件：一个资源每次只能被一个进程使用。即在一段时间内某资源仅为一个进程所占有。此时若有其他进程请求该资源，则请求进程只能等待。

请求与保持条件：一个进程因请求资源而阻塞时，对已获得的资源保持不放。即进程已经保持了至少一个资源，但又提出了新的资源请求，而该资源已被其他进程占有，此时请求进程被阻塞，但对自己已获得的资源保持不放。

不可剥夺条件：进程所获得的资源在未使用完毕之前，不能被其他进程强行夺走。即只能由获得该资源的进程自己来释放（只能是主动释放)。

循环等待条件： 若干进程间形成首尾相接循环等待资源的关系。

这四个条件是死锁的必要条件，只要系统发生死锁，这些条件必然成立，而只要上述条件之一不满足，就不会发生死锁。

进程死锁和线程死锁的区别就是主语不同而已。

#### 死锁的预防与解决

在有些情况下死锁是可以避免的。三种用于避免死锁的技术：

1. 加锁顺序（线程按照一定的顺序加锁）
2. 加锁时限（线程尝试获取锁的时候加上一定的时限，超过时限则放弃对该锁的请求，并释放自己占有的锁）
3. 死锁检测

> 解决死锁的实际案例

Todo 

#### atomic修饰符

原子性是指一个事物的操作是不可分割的，要么都发生，要么都不发生。有点量子理论的意思。

使用atomic修饰的属性，告诉编译器在合成getter/setter方法时，需要加锁。比如线程A在setter时，线程B想getter那么就需要等待。

atomic只是保证了属性的读写安全，但无法保证对象的线程安全。

[Properties Are Atomic by Default](https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ProgrammingWithObjectiveC/EncapsulatingData/EncapsulatingData.html#//apple_ref/doc/uid/TP40011210-CH5-SW37)  官方文档

[关于IOS 属性atomic（原子性）的理解](https://juejin.im/post/5c25f0d9f265da613b6fd84b)

考虑XYZPerson，拥有firstName和lastName两个atomic属性。如果一个线程A正在修改这两个属性，与此同时另一个线程B正在读取这两个属性，那么线程B得到的firstName和lastName的值将会发生数据不匹配的情况。

eg：

初始时, firstName = @"李";  lastName = @"四";

线程A想修改为：firstName = @"张";  lastName = @"三";

与此同时线程B在读取这两个属性，那么线程B有可能得到：

张四，李三。

要想解决的话就应该把`firstName = @"张";  lastName = @"三";`这一段代码也加锁。

另外一个例子：
经典的i++问题，即使i是一个atomic属性，i++也不是线程安全的。

i++对应的汇编：

```
movl -0x24(%rbp), %r8d
addl $0x1, %r8d
movl %r8d, -0x24(%rbp)
```

完成一次`i++`总共分为三步：

- 取出`i`存放到寄存器上
- 对寄存器的值`+1`
- 将计算后的值存放回`i`的内存

参考：[多线程-线程安全](http://sindrilin.com/2017/09/09/thread_safe.html)

具体实现可以去看atomic的源码。

#### 参考

[iOS多线程安全-13种线程锁🔒](https://juejin.im/post/5bf21d935188251d9e0c2937)

[[iOS] 谈谈iOS多线程的锁](https://juejin.im/post/5a0a92996fb9a0451f307479)

[iOS 多线程之线程安全](https://juejin.im/post/5c49655551882524a5421d19)

[深入理解 iOS 开发中的锁](https://juejin.im/post/57f6e9f85bbb50005b126e5f)

[iOS 锁的底层探索笔记](https://juejin.im/post/5e60b5caf265da5726610e58)

比较专业的分析

[不再安全的 OSSpinLock](https://blog.ibireme.com/2016/01/16/spinlock_is_unsafe_in_ios/)，YYKit的作者写的一篇博客，很不错。

[@synchronized 内幕揭秘](https://jianli2017.top/wiki/IOS/Runtime/objc/18_syncsize/)

[pthread的各种同步机制](https://casatwy.com/pthreadde-ge-chong-tong-bu-ji-zhi.html)，casa写的一篇博客，虽然案例很多，不过是真的看不懂。

案例:

[从一道网易面试题浅谈OC线程安全](https://blog.csdn.net/u013883974/article/details/77645212)
看来内存管理ARC和MRC必须都知道,还有自动释放池的底层原理.

